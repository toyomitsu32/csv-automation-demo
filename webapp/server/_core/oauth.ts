import type { Express } from "express";

// OAuth routes are disabled in this version
// ID/Password authentication is used instead
export function registerOAuthRoutes(_app: Express) {
  // No OAuth routes registered
}
